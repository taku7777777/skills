---
name: skill-improvement
description: Claude Code スキルを評価駆動で作成・測定・改善するスキル。新規スキルの作成(評価を先に作る)、既存スキルへの評価セット(evals.md)の整備、発火しない・効果が分からないスキルの診断、実行で観測した失敗のスキルへの反映(蒸留)、編集の採否判定に使用する。トリガー例:「スキルの評価セットを作って」「このスキルが効いているか測定して」「スキルが発火しない」「スキルを改善して」「この失敗をスキルに反映して」「新しいスキルを作りたい」
---

# スキル評価・改善スキル

Anthropic のスキル作成ベストプラクティス(評価を先に作る反復プロセス、skill-creator)、スキル自動最適化研究(実行トラジェクトリからの蒸留、検証信号の設計、厳密改善時のみ採用)、公開スキルの効果実測調査の知見を統合した、スキルを「書きっぱなし」にしないための評価・改善運用プロセス。

対象は Claude Code のスキル(SKILL.md + references)。エージェント委任全般の検証ゲート設計は `harness-engineering`、ドキュメント体系そのものは `docs-for-ai` の領分。本スキルは「スキルという1つの成果物」の効果測定と改善に特化する。

## 大原則

1. **評価が先、本文が後。** Anthropic の公式プロセスは「ギャップ特定 → 検証シナリオ作成 → ベースライン測定 → 最小限の記述 → 反復」であり、広範な本文を書く前に評価を作れと明言している。既存スキルへの後付け(retrofit)でも順序は同じ — まず評価、それから編集。
2. **測定していないスキルの効果を仮定しない。** 公開ベンチマークでは、既存スキルの多くが対象条件でpass rateを改善せず、一部は劣化した。結果はモデル・リポジトリ・タスク・スキル版に依存するため、外部の平均値を本スキルの効果へ転用しない。「丁寧に書いた」ことではなく、同一条件のbaseline比較を根拠にする(研究の版と適用範囲は `references/evidence.md`)。
3. **スキルは、モデル重みを変えずに挙動を調整できる主要な推論時制御面である。** モデル、system prompt、tool policy、取得文脈など他の要因と切り分けて評価する。実行記録を観測データ、検証シナリオを評価関数、テキスト編集を更新候補として扱う比喩は有用だが、勾配降下と同じ保証があるわけではない。
4. **発火しないスキルは、存在しないのと同じ。** 起動時にコンテキストへ載るのは name と description のみで、発火判断はそこだけで決まる。本文の充実より先に、発火テストが通ることを確認する。
5. **反復評価で厳密改善した場合のみ採用する。** 編集後に同一条件のbaseline/with-skillを複数回実行し、重要項目の悪化なしで改善した場合だけ取り込む。総得点が上がってもセキュリティ・データ損失・誤発火等のcritical条件が悪化したら差し戻す。また**1回きりの失敗への対症編集は過適合** — 繰り返し観測された失敗パターンだけを本文に反映する。
6. **蒸留は使った直後が最も安い。** スキルを使ったセッションの終了前に、成功したアプローチとつまずいた箇所をそのセッション自身に要約させ、編集候補として記録する。後日ゼロから思い出すより桁違いに安く、正確である。
7. **機械判定に落とせない評価は、人間の仕事として明示的に残す。** 成果物の専門的な質の判断を無理に自動化すると損失関数が壊れ、評価全体が無意味になる。「(人間判定)」と書いて残すことは失敗ではなく設計である。

## 評価セットの規約

各スキルはディレクトリ直下に `evals.md` を持つ。構成は3部: **発火テスト**(発火すべきプロンプト5+発火すべきでないプロンプト3)、**検証シナリオ3つ**(タスク / スキルなしで予想される欠落 / 合格条件チェックリスト)、**実行記録**(日付・モデル・条件・判定の追記型ログ)。実行可能なraw fixtureとgrader rubricは、インストール対象から分離したリポジトリレベルの評価領域に置く。テンプレートと情報漏洩を防ぐ実行方法は `references/eval-template.md` に従う。

## ワークフロー

### ステップ0: モードの判定

依頼を4モードに分類する: **(A) 新規スキル作成**(ステップ1から順に)/ **(B) 既存スキルへの評価整備**(ステップ2から)/ **(C) 改善反復**(ステップ5から。評価がなければ先にBを実施)/ **(D) 発火診断**(ステップ2の発火テストのみ整備・実行し、description を修正)。

### ステップ1: ギャップの特定(新規作成時)

スキルなしの素のモデルに代表タスクを実行させ、**具体的にどこで失敗するか**を記録する。失敗が観測できないなら、そのスキルは作らない(一般知識で足りている)。失敗の記録がそのまま検証シナリオの「スキルなしで予想される欠落」になる。

### ステップ2: 評価セットの作成

`references/eval-template.md` に従い `evals.md` を書く。要点: 発火すべきプロンプトは description のトリガー例の言い換えではなくユーザーが実際に打ちそうな文にする / 発火すべきでないプロンプトは隣接スキルとの境界を突く / 検証シナリオは worked-example と別題材にする / 合格条件は第三者が Yes/No 判定できる形に寄せる。

### ステップ3: ベースライン測定

スキルなしの新セッションで検証シナリオを複数回実行し、成果物を保存・独立採点して実行記録に残す。実行エージェントにはraw taskとfixtureだけを渡し、期待答え・「予想される欠落」・rubric・過去出力を見せない。**baseline が全条件を満たすシナリオは検出力ゼロ**なので書き直す。ここを飛ばすと「スキルの効果」が永遠に測れない。

### ステップ4: 最小限の記述・編集

ギャップに対処する**最小限**の内容を書く。網羅的な教科書ではなく、素のモデルが自発的にやらないことだけを足す。分量が増えるほど発火後のコンテキスト税が上がる — SKILL.md は簡潔に保ち、詳細は references へ(progressive disclosure)。

### ステップ5: 評価実行と採否判定

with-skill 条件をbaselineと同じモデル・tool policy・fixture・反復数で実行し、別セッションまたは人間がblind採点する。平均得点だけでなくcritical失敗、発火精度、トークン、レイテンシ、費用、tool errorを比較する。**全シナリオを完了し、critical失敗とtool errorの悪化なしで反復結果が改善した場合のみ編集を採用**し、実行記録に追記する。改善しない場合は差し戻し、ギャップ仮説・シナリオ設計・モデルとの相性を疑う。

### ステップ6: 継続運用(蒸留と定期見直し)

スキルを使ったセッションから失敗パターンを蒸留して編集候補に積み、**繰り返し現れたものだけ**ステップ4〜5の反復にかける。四半期ごとに実行記録を見直し、(a) 常に合格するシナリオの難化、(b) 発火テストの実績確認、(c) 出典の陳腐化チェックを行う。

## 成果物フォーマット

- モードA/B: 対象スキルの `evals.md`(テンプレート準拠)+ ベースライン測定の実行記録。
- モードC: 編集diff + 編集前後の評価結果比較表(反復数、critical失敗、発火精度、トークン/時間/費用、tool errorを含む)+ 採否判定(採用/差し戻し+理由)。
- モードD: 発火テスト結果表 + description の修正案(修正理由: どのプロンプトが不発/誤発火だったか)。

実例: `references/worked-example.md`

## 知見の出典

- Anthropic「Skill authoring best practices」および skill-creator(評価を先に作る反復プロセス、progressive disclosure、最小限の記述)
- スキル評価・自動最適化の研究(2026年上半期): 実行トラジェクトリからの蒸留、繰り返し現れる診断の保持、検証信号の設計、paired control/treatment評価。いずれも新しい研究であり、一般化範囲と査読状況を区別する
- SWE-Skills-Bench: 公開スキルを固定リポジトリと実行可能な受け入れ条件で比較し、スキルの存在だけでは改善を保証しないことを示す
- 深層学習の訓練ループとの構造対応(パラメータ=SKILL.md、訓練データ=実行記録、損失関数=検証シナリオ、過学習監視=採否判定)

一次URL、版、アクセス日、査読状況は `references/evidence.md` を参照。
